# Bundled runtime + deps (recreated by npm install) — not committed
node/
node_modules/

# Packaged distributable output (produced by npm run build-exe)
build/dist/

# Tool output / working saves
*-REALCOACHES
*-REALCOACHES.backup
reference-save
